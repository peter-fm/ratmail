version: 1
metadata:
  display_name: Ratmail CLI
  short_description: Run ratmail CLI commands and configure safe JSON output.
  default_prompt: Operate the ratmail CLI safely, configure minimal access in ratmail.toml, and provide jq-friendly JSON guidance.
